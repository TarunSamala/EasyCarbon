// ListPage.js

import React from 'react';
import MintTokens from './MintTokens';

const ListPage = () => {
    return (
        <div>
            <h2>List Page</h2>
            <MintTokens />
        </div>
    );
};

export default ListPage;
